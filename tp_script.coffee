#transcriptome analysis
# go to dataset working direcrory with the command cd for change directory:
cd dataset
# list all the content of the dataset folder with ls command:
ls
# go into the FastQC folder:
cd FastQC
# run the fastqc tool:
./fastqc
